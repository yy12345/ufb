<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
     xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xmlns:aop="http://www.springframework.org/schema/aop"
     xmlns:tx="http://www.springframework.org/schema/tx"
     xmlns:jdbc="http://www.springframework.org/schema/jdbc"
     xmlns:context="http://www.springframework.org/schema/context"
     xsi:schemaLocation="
     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.0.xsd
     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
     http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">

	<!-- 数据源配置 
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"  destroy-method="close">
		<property name="driverClassName" value="${ufb.jdbc.driverClassName}" />
		<property name="url" value="${ufb.jdbc.url}"/>
		<property name="username" value="${ufb.jdbc.username}" />
		<property name="password" value="${ufb.jdbc.password}" />
		<property name="testOnBorrow" value="false" />
		<property name="testWhileIdle" value="true" />
		&lt;!&ndash; 连接池启动的初始值 &ndash;&gt;
		<property name="initialSize" value="${ufb.dbcp.initialSize}" />
		&lt;!&ndash; 连接池最大值 &ndash;&gt;
		<property name="maxActive" value="${ufb.dbcp.maxActive}" />
		&lt;!&ndash; 最大空闲值。当经过一个高峰时间后，连接池可以慢慢将已经用不到的连接慢慢释放一部分，一直减少到maxIdle为止 &ndash;&gt;
		<property name="maxIdle" value="${ufb.dbcp.maxIdle}" />
		
		&lt;!&ndash; 最小空闲值。当空闲的连接数少于阀值时，连接池就会预申请去一些连接，以免洪峰来时来不及申请 &ndash;&gt;
		<property name="minIdle" value="${ufb.dbcp.minIdle}" />
		&lt;!&ndash;#给出一条简单的sql语句进行验证&ndash;&gt;
		<property name="validationQuery" value="${ufb.dbcp.testsql}" />
		&lt;!&ndash;#在取出连接时进行有效验证&ndash;&gt;
		<property name="removeAbandonedTimeout" value="${ufb.dbcp.removeAbandonedTimeout}" />
		<property name="removeAbandoned" value="${ufb.dbcp.removeAbandoned}" />
		&lt;!&ndash; #运行判断连接超时任务的时间间隔，单位为毫秒，默认为-1,即不执行任务。 &ndash;&gt;
		<property name="timeBetweenEvictionRunsMillis" value="${ufb.dbcp.timeBetweenEvictionRunsMillis}" />
		&lt;!&ndash; #连接的超时时间，默认为半小时。 &ndash;&gt;
		<property name="minEvictableIdleTimeMillis" value="${ufb.dbcp.minEvictableIdleTimeMillis}" />	
	</bean> -->
	
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"  destroy-method="close">
		<property name="driverClassName" value="com.mysql.jdbc.Driver" />
		<property name="url" value="jdbc:mysql://127.0.0.1:3306/test"/>
		<property name="username" value="root" />
		<property name="password" value="" />
	</bean>

    <!-- transaction manager, use JtaTransactionManager for global tx -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- define the SqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="typeAliasesPackage" value="com.ufufund.trade.ufb.model, com.ufufund.trade.ufb.model.db" />
    </bean>

    <!-- scan for mappers and let them be autowired -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.ufufund.trade.ufb.dao" />
    </bean>
    
    <!-- enable transaction demarcation with annotations -->
    <tx:annotation-driven />
</beans>